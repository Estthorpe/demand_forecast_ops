{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "dvc remote modify origin --local auth basic || true && dvc remote modify origin --local user $DAGSHUB_USERNAME || true && dvc remote modify origin --local password $DAGSHUB_TOKEN || true && dvc pull || true && uvicorn src.serving.app:app --host 0.0.0.0 --port 8000 --workers 1"
  }
}
