# ─────────────────────────────────────────────────────────────────────────────
# demand-forecast-ops — requirements.txt
# Direct dependencies only. Pinned for reproducibility.
# Install: pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

# ── Configuration & environment ──────────────────────────────────────────────
pydantic==2.7.1
pydantic-settings==2.2.1
python-dotenv==1.0.1

# ── Logging ──────────────────────────────────────────────────────────────────
loguru==0.7.2

# ── Data engineering ─────────────────────────────────────────────────────────
pandas==2.2.2
numpy==1.26.4
pyarrow==15.0.0          # parquet read/write — used by pandas and DVC

# ── Time series & statistical modelling ──────────────────────────────────────
statsmodels==0.14.2      # ARIMA baseline
lightgbm==4.3.0          # primary forecasting model
scikit-learn==1.4.2      # preprocessing utilities, metrics

# ── MLflow experiment tracking ───────────────────────────────────────────────
mlflow==2.13.0

# ── Data version control ─────────────────────────────────────────────────────
dvc==3.50.1

# ── API serving ──────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn==0.29.0          # ASGI server — runs FastAPI locally
httpx==0.27.0            # async HTTP client — used in tests to call FastAPI

# ── Data validation / contracts ──────────────────────────────────────────────
# pydantic already listed above — covers all Pydantic validation needs

# ── Code quality ─────────────────────────────────────────────────────────────
ruff==0.4.4
mypy==1.10.0
pre-commit==3.7.1

# ── Testing ──────────────────────────────────────────────────────────────────
pytest==8.2.0
pytest-cov==5.0.0        # test coverage reporting

# ── GenAI / LLM (Week 4 only — safe to install now, not called until Week 4) ─
anthropic==0.26.0        # Claude API — used in narrative generator
